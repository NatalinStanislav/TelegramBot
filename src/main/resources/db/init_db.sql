CREATE TABLE player
(
    id               VARCHAR     PRIMARY KEY    NOT NULL,
    name             VARCHAR                    NOT NULL,
    total_score      VARCHAR                    NOT NULL,
    goal             VARCHAR                    NOT NULL,
    pass             VARCHAR                    NOT NULL,
    penalty          VARCHAR                    NOT NULL
);